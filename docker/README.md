# Docker Configurations

This directory contains Docker configuration placeholders for OpenSource Scout project.

- `Dockerfile.client`: Frontend client container configuration.
- `Dockerfile.server`: Backend server container configuration.
- `docker-compose.yml`: Multi-container orchestrator configuration.
